# Coding Rules

- Use `declare(strict_types=1);` in PHP files.
- Keep controllers thin and validation in form requests.
- Prefer service classes for business workflows.

## Universal Rules
- Preserve existing repository style unless changing it is part of the task.
- Avoid dead code, commented-out experiments, and unrelated refactors.
- Keep changes narrow and verifiable.
