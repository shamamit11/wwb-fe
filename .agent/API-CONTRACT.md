# API Contract Guidelines

- Keep request and response contracts explicit.
- Validate input at the transport boundary.
- Never leak internal-only fields or secrets through public payloads.
- Update source-of-truth docs when the contract changes.
